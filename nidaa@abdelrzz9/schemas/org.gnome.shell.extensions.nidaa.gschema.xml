<?xml version="1.0" encoding="utf-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.nidaa"
          path="/org/gnome/shell/extensions/nidaa/">

    <!-- ======== Location ======== -->
    <key name="location-mode" type="s">
      <default>'auto'</default>
      <summary>Location resolution method</summary>
      <description>
        How to determine the user's location.
        'auto' — try Geoclue first, then IP fallback, then cached.
        'manual' — use the lat/lng set in manual-latitude / manual-longitude.
        'city' — use the bundled city database (manual-latitude / manual-longitude set automatically).
      </description>
    </key>

    <key name="manual-latitude" type="d">
      <default>0.0</default>
      <summary>Manual latitude</summary>
      <description>Latitude used when location-mode is 'manual' or 'city'.</description>
    </key>

    <key name="manual-longitude" type="d">
      <default>0.0</default>
      <summary>Manual longitude</summary>
      <description>Longitude used when location-mode is 'manual' or 'city'.</description>
    </key>

    <key name="manual-city-name" type="s">
      <default>''</default>
      <summary>Selected city name</summary>
      <description>Display-only: name of the city selected via the city search.</description>
    </key>

    <!-- ======== Prayer calculation ======== -->
    <key name="prayer-method" type="i">
      <default>0</default>
      <summary>Calculation method</summary>
      <description>
        Prayer time calculation method.
        0 = Muslim World League, 1 = ISNA, 2 = Egypt, 3 = Umm al-Qura,
        4 = Karachi, 5 = Tehran, 6 = Jafari, 7 = Custom.
      </description>
    </key>

    <key name="asr-method" type="i">
      <default>0</default>
      <summary>Asr calculation method</summary>
      <description>0 = Shafii (standard), 1 = Hanafi.</description>
    </key>

    <key name="high-latitude-method" type="i">
      <default>3</default>
      <summary>High latitude rule</summary>
      <description>
        0 = None, 1 = Middle of Night, 2 = One-Seventh of Night, 3 = Angle-Based.
      </description>
    </key>

    <key name="fajr-angle" type="d">
      <default>18.0</default>
      <summary>Fajr angle in degrees</summary>
      <description>Only used when prayer-method is Custom.</description>
    </key>

    <key name="isha-angle" type="d">
      <default>17.0</default>
      <summary>Isha angle in degrees</summary>
      <description>Only used when prayer-method is Custom (and method does not use fixed minutes).</description>
    </key>

    <!-- ======== Per-prayer manual offsets ======== -->
    <key name="offset-fajr" type="i">
      <default>0</default>
      <summary>Fajr time offset in minutes</summary>
      <description>Manual minutes adjustment applied to calculated Fajr time. Positive = later, negative = earlier.</description>
    </key>

    <key name="offset-dhuhr" type="i">
      <default>0</default>
      <summary>Dhuhr time offset in minutes</summary>
      <description>Manual minutes adjustment applied to calculated Dhuhr time.</description>
    </key>

    <key name="offset-asr" type="i">
      <default>0</default>
      <summary>Asr time offset in minutes</summary>
      <description>Manual minutes adjustment applied to calculated Asr time.</description>
    </key>

    <key name="offset-maghrib" type="i">
      <default>0</default>
      <summary>Maghrib time offset in minutes</summary>
      <description>Manual minutes adjustment applied to calculated Maghrib time.</description>
    </key>

    <key name="offset-isha" type="i">
      <default>0</default>
      <summary>Isha time offset in minutes</summary>
      <description>Manual minutes adjustment applied to calculated Isha time.</description>
    </key>

    <!-- ======== Notifications ======== -->
    <key name="notifications-enabled" type="b">
      <default>true</default>
      <summary>Enable prayer notifications</summary>
    </key>

    <key name="notify-fajr" type="b">
      <default>true</default>
      <summary>Notify at Fajr time</summary>
    </key>

    <key name="notify-dhuhr" type="b">
      <default>true</default>
      <summary>Notify at Dhuhr time</summary>
    </key>

    <key name="notify-asr" type="b">
      <default>true</default>
      <summary>Notify at Asr time</summary>
    </key>

    <key name="notify-maghrib" type="b">
      <default>true</default>
      <summary>Notify at Maghrib time</summary>
    </key>

    <key name="notify-isha" type="b">
      <default>true</default>
      <summary>Notify at Isha time</summary>
    </key>

    <key name="adhan-volume" type="d">
      <default>0.8</default>
      <summary>Volume for adhan playback (0.0 – 1.0)</summary>
    </key>

    <key name="prayer-snooze-duration" type="i">
      <default>10</default>
      <summary>Snooze duration in minutes</summary>
      <description>How long to snooze a prayer notification before re-alerting.</description>
    </key>

    <key name="prayer-iqamah-reminder-offset" type="i">
      <default>0</default>
      <summary>Iqamah reminder offset in minutes</summary>
      <description>
        Minutes after the adhan to fire an iqamah reminder.
        0 disables the iqamah reminder.
      </description>
    </key>

    <key name="prayer-ending-soon-offset" type="i">
      <default>0</default>
      <summary>Prayer-ending-soon warning offset in minutes</summary>
      <description>
        Minutes before the next prayer to fire a "prayer time ending soon" warning.
        0 disables the warning.
      </description>
    </key>

    <key name="prayer-sound-fajr" type="s">
      <default>''</default>
      <summary>Sound file path for Fajr adhan</summary>
    </key>

    <key name="prayer-sound-dhuhr" type="s">
      <default>''</default>
      <summary>Sound file path for Dhuhr adhan</summary>
    </key>

    <key name="prayer-sound-asr" type="s">
      <default>''</default>
      <summary>Sound file path for Asr adhan</summary>
    </key>

    <key name="prayer-sound-maghrib" type="s">
      <default>''</default>
      <summary>Sound file path for Maghrib adhan</summary>
    </key>

    <key name="prayer-sound-isha" type="s">
      <default>''</default>
      <summary>Sound file path for Isha adhan</summary>
    </key>

    <!-- ======== Adhkar ======== -->
    <key name="adhkar-enabled" type="b">
      <default>true</default>
      <summary>Enable adhkar reminders</summary>
    </key>

    <key name="adhkar-language" type="s">
      <default>'en'</default>
      <summary>Translation language for adhkar</summary>
      <description>'en' for English, 'fr' for French.</description>
    </key>

    <key name="adhkar-morning-offset" type="i">
      <default>15</default>
      <summary>Morning adhkar offset after sunrise</summary>
      <description>Minutes after sunrise to fire the morning adhkar reminder. 0 = at sunrise.</description>
    </key>

    <key name="adhkar-evening-offset" type="i">
      <default>15</default>
      <summary>Evening adhkar offset before Maghrib</summary>
      <description>Minutes before Maghrib to fire the evening adhkar reminder. 0 = at Maghrib.</description>
    </key>

    <key name="adhkar-post-prayer-offset" type="i">
      <default>30</default>
      <summary>Post-prayer adhkar offset</summary>
      <description>Minutes after each prayer to fire the post-prayer adhkar reminder. 0 = immediately after prayer time.</description>
    </key>

    <key name="adhkar-post-fajr" type="b">
      <default>true</default>
      <summary>Post-prayer adhkar after Fajr</summary>
    </key>

    <key name="adhkar-post-dhuhr" type="b">
      <default>true</default>
      <summary>Post-prayer adhkar after Dhuhr</summary>
    </key>

    <key name="adhkar-post-asr" type="b">
      <default>true</default>
      <summary>Post-prayer adhkar after Asr</summary>
    </key>

    <key name="adhkar-post-maghrib" type="b">
      <default>true</default>
      <summary>Post-prayer adhkar after Maghrib</summary>
    </key>

    <key name="adhkar-post-isha" type="b">
      <default>true</default>
      <summary>Post-prayer adhkar after Isha</summary>
    </key>

    <!-- ======== Quran Reading ======== -->
    <key name="quran-enabled" type="b">
      <default>true</default>
      <summary>Enable Quran reading reminders</summary>
    </key>

    <key name="quran-frequency" type="s">
      <default>'daily'</default>
      <summary>Quran reminder frequency</summary>
      <description>
        'daily' — once per day
        'weekly' — once per week (Fridays)
        'after-fajr' — after Fajr prayer
        'after-isha' — after Isha prayer
        'every-6h' — every 6 hours
        'random' — random time within a user-set window
      </description>
    </key>

    <key name="quran-daily-goal" type="i">
      <default>5</default>
      <summary>Daily page goal</summary>
      <description>Number of Quran pages to aim for each day.</description>
    </key>

    <key name="quran-offset" type="i">
      <default>30</default>
      <summary>Offset after prayer for Quran reminder</summary>
      <description>Minutes after Fajr/Isha to send the Quran reminder (only for after-fajr/after-isha frequencies).</description>
    </key>

    <key name="quran-window-start" type="i">
      <default>8</default>
      <summary>Random window start hour</summary>
      <description>Start of the random reminder window (0-23). Only used when frequency is 'random'.</description>
    </key>

    <key name="quran-window-end" type="i">
      <default>21</default>
      <summary>Random window end hour</summary>
      <description>End of the random reminder window (0-23). Only used when frequency is 'random'.</description>
    </key>

    <!-- ======== Qibla ======== -->
    <key name="qibla-latitude" type="d">
      <default>21.4225</default>
      <summary>Kaaba latitude for Qibla calculation</summary>
    </key>

    <key name="qibla-longitude" type="d">
      <default>39.8262</default>
      <summary>Kaaba longitude for Qibla calculation</summary>
    </key>

  </schema>
</schemalist>
